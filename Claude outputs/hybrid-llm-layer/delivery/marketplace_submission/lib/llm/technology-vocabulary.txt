# Technology names a semantic result may not assert unless the cited evidence
# names them too.  Kept as data rather than as a constant in validation.py for
# two reasons: it is a list a reviewer will want to edit without touching code,
# and shipping vendor tokens inside skill source trips the package validator's
# "no vendor-specific selector" scan, which exists to stop detectors being
# fitted to particular platforms.  This list is the opposite of a detector - it
# is a denylist of claims - but a regex cannot tell those apart, so the two are
# kept physically separate.
#
# One lowercase name per line; blank lines and # comments are ignored.

react
next.js
nextjs
vue
nuxt
angular
svelte
ember
wordpress
woocommerce
magento
drupal
joomla
wix
webflow
hubspot
salesforce
django
rails
laravel
cloudflare
akamai
fastly
jquery
bootstrap
tailwind
graphql
algolia
contentful
sanity
strapi
